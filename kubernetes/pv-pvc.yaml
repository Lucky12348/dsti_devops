apiVersion: v1
kind: PersistentVolume
metadata:
  name: dsti-devops-pv
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: manual
  hostPath:
    path: /mnt/data/dsti-devops
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dsti-devops-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: manual
